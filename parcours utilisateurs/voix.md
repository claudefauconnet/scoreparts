Pour l'instant on force l'utilisateur à remplir les voix avant de se lancer dans les zones.

Instruments = voix par systeme

zone = mode zone des qu'on clique ca envoie 

partition complete = assembler voix 

selectionner toutes les zones + selectionner toutes les voix

réduire tout